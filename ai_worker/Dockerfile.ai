FROM python:3.11

WORKDIR /app

COPY ai_worker/requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r /app/requirements.txt

COPY ai_worker /app/ai_worker
COPY common /app/common

ENV PYTHONPATH=/app
CMD ["python", "-m", "ai_worker.run_ai_worker"]
